import{a,t as o}from"./disclose-version.Z0CCbWqE.js";import"./legacy.Cu7IsEwU.js";import{D as i}from"./runtime.D0xmrmwk.js";const p={layout:"post",title:"Building a Potrace-style Bitmap Tracer",summary:"Implementing bitmap-to-SVG tracing in JavaScript — edge maps, closed-polygon RDP, and Bézier fitting",date:"2026-03-29T00:00:00.000Z"};var r=o('<p>A bitmap tracer converts a raster image (pixels) into a smooth vector graphic (SVG paths). The classic tool for this is <a href="https://potrace.sourceforge.net/" rel="nofollow">Potrace</a>, and this post walks through re-implementing its core algorithm in JavaScript.</p> <h2>The pipeline</h2> <p>The tracer runs in five stages, each implemented as a generator so you can pause and inspect the intermediate state at any step:</p> <ol><li><strong>Threshold</strong> — convert RGBA pixels to a 1-bit bitmap</li> <li><strong>Trace</strong> — extract closed contours as sequences of corner points</li> <li><strong>Simplify</strong> — reduce contours to lightweight polygons via Ramer-Douglas-Peucker</li> <li><strong>Bézier fit</strong> — replace polygon edges with cubic Bézier curves</li> <li><strong>SVG</strong> — serialize to an SVG <code>&lt;path&gt;</code> element</li></ol> <h2>Tracing on a half-integer grid</h2> <p>The key insight is to trace <em>edges between pixels</em> rather than pixels themselves. Each foreground pixel occupies a unit square on a half-integer grid; the boundary between a foreground and background pixel is a directed unit edge. We build a map of all such edges and stitch them into closed polygons in one pass.</p> <p>This gives exact, closed outlines with no ambiguity about winding order — holes are detected automatically via the shoelace signed-area formula.</p> <h2>Closed-polygon RDP</h2> <p>Standard Ramer-Douglas-Peucker locks its endpoints, so applying it to a closed polygon always leaves an artifact at the seam. The fix: find the two maximally-distant points in the polygon, split there, and run open RDP on each half independently.</p> <h2>Least-squares Bézier fitting</h2> <p>Between detected corners, we fit a cubic Bézier using the Schneider (1990) least-squares method: parameterize the arc by chord length, then solve a 2×2 linear system for the two control-point offsets α₁ and α₂. If the fit error exceeds a threshold, split at the worst point and recurse.</p> <h2>Try it</h2> <p>The interactive debugger below lets you upload any image, tune the parameters, and scrub through every step of the pipeline:</p> <a href="/potrace" class="inline-block mt-2 underline">Open the step-through debugger →</a>',1);function h(e){var t=r();i(26),a(e,t)}export{h as default,p as metadata};
